// `app/page.js` is the UI for the root `/` URL

// page.tsx file is necessary to make a route segment public

//  pages => to show UI that is shared across multiple routes.
//  layout => show UI that is shared across multiple routes.

export default function Page() {
	return <h1>Home Page</h1>;
}
